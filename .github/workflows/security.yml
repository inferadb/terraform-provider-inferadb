# yaml-language-server: $schema=https://www.schemastore.org/github-workflow.json

name: Security

on:
  pull_request:
    branches: [main]

# Cancel in-progress runs when new commits are pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  # Detect if security-relevant files changed
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      go: ${{ steps.filter.outputs.go }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Check for dependency changes
        uses: step-security/paths-filter@6eee183b0d2fd101d3f8ee2935c127bca14c5625 # v3.0.5
        id: filter
        with:
          filters: |
            go:
              - 'go.mod'
              - 'go.sum'
              - '.github/workflows/security.yml'

  dependency-review:
    name: Dependency Review
    needs: [detect-changes]
    runs-on: ubuntu-latest
    # Only run if Go dependency files changed AND not triggered by dependabot
    if: needs.detect-changes.outputs.go == 'true' && github.actor != 'dependabot[bot]'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Dependency Review
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4.9.0
        with:
          fail-on-severity: high
          comment-summary-in-pr: always

  # Security scan summary - aggregates all security job results
  # This job ALWAYS runs to satisfy required status checks
  security-summary:
    name: Security Summary
    needs: [detect-changes, dependency-review]
    runs-on: ubuntu-latest
    if: always()
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - name: Check security scan results
        env:
          GO_CHANGES: ${{ needs.detect-changes.outputs.go }}
          DEPENDENCY_REVIEW_RESULT: ${{ needs.dependency-review.result }}
        run: |
          echo "## Security Scan Results"
          echo ""

          # If no security-relevant files changed, report success immediately
          if [[ "$GO_CHANGES" != "true" ]]; then
            echo "No Go dependency changes detected - security scan not required."
            echo ""
            echo "✅ Security checks passed (no relevant changes)"
            exit 0
          fi

          echo "| Scanner | Status |"
          echo "|---------|--------|"
          echo "| Dependency Review | $DEPENDENCY_REVIEW_RESULT |"
          echo ""

          # Fail if any security job failed
          if [[ "$DEPENDENCY_REVIEW_RESULT" != "success" && "$DEPENDENCY_REVIEW_RESULT" != "skipped" ]]; then
            echo "❌ Security checks failed"
            exit 1
          fi

          echo "✅ All security checks passed!"
